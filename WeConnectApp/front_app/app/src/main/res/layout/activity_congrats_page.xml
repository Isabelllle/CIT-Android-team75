<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".CongratsPage"
    android:background="@drawable/congrats_background"
    >
    <ImageView
        android:id="@+id/survey_base_logo"
        android:layout_width="80dp"
        android:layout_height="80dp"
        android:layout_centerHorizontal="true"
        android:contentDescription="@string/logo"
        android:scaleX="0.7"
        android:scaleY="0.7"
        app:layout_constraintHorizontal_bias="0.498"
        app:srcCompat="@drawable/logo" />

    <ImageView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/congrats_image"
        android:src="@drawable/congrats_image"
        android:layout_below="@id/survey_base_logo"
        android:layout_centerInParent="true"
        android:layout_marginTop="150dp"/>

    <TextView
        android:id="@+id/title_congrats"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/congrats_image"
        android:layout_centerInParent="true"
        android:layout_marginTop="20dp"
        android:fontFamily="@font/baloo_bhaijaan"
        android:text="@string/congrats"
        android:textColor="@color/black"
        android:textSize="45sp" />
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/subtitle_congrats"
        android:text="@string/thanks_for_submit"
        android:layout_below="@id/title_congrats"
        android:layout_centerInParent="true"
        android:textSize="16sp"
        />
    <Button
        android:id="@+id/return_btn"
        android:layout_width="200dp"
        android:layout_height="wrap_content"
        android:background="@drawable/survey_detail_btn_templet"
        android:layout_below="@id/subtitle_congrats"
        android:layout_centerInParent="true"
        android:layout_marginTop="30dp"
        android:text="@string/return_home_page"
        android:textColor="@color/white"
        />


</RelativeLayout>